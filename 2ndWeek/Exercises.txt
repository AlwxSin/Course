1) Напишите функцию input_int(a, b), которая будет работать так же как функция input() без параметров, но только она будет проверять, что введённая пользователем строка действительно является числом в интервале [a;b], и если это не так, то будет просить пользователя повторить попытку ввода.

2) Напишите функцию maximum_prime(n), которая принимает на вход какое-то натуральное число, а возвращает в качестве результата его наибольший простой множитель

3) По аналогии с тем, как я поступил с факториалом, сделайте вывод каждого шага вычисления функции fibo на экран, чтобы понять что реально тут происходит и почему функция работает медленно.

4) Напишите функцию fibo так, чтобы она вычислялась эффективно, то есть без рекурсии.

5) Когда напишите свою реализацию fibo, используйте её для сравнения скорости вычисления моей и вашей функции.